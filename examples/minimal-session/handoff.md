# Handoff

The provider-neutral package schema and validator are in progress.

Continue by implementing the first source adapter, preserving explicit
capability-loss reporting at the adapter boundary.
